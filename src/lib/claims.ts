import Ability, { buildAbility } from "./claims/ability";
import Claim, { ALLOWED_VERBS, buildClaim } from "./claims/claim";
import ClaimSet, { buildClaimSet } from "./claims/claim-set";

export default {
  ALLOWED_VERBS,
  Ability,
  Claim,
  ClaimSet,
  buildAbility,
  buildClaim,
  buildClaimSet
};
